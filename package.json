{
  "name": "stepcheck",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "StepCheck — a CBSE Class 10 maths step-verifier that checks a student's own working, finds the first wrong step, and never reveals the answer.",
  "scripts": {
    "dev": "vite",
    "dev:api": "node scripts/dev-api.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
